<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
        android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

        <ScrollView
            android:layout_width="fill_parent"
            android:layout_height="fill_parent">

            <RelativeLayout
                android:layout_width="fill_parent"
                android:layout_height="fill_parent">

                <TextView
                    android:id="@+id/compNameLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="30dp"
                    android:textSize="22dp"
                    android:text="FTC World Championships"
                    android:textAppearance="@style/BoldText"/>
                <TextView
                    android:id="@+id/compLocLabel"
                    android:layout_below="@id/compNameLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="26dp"
                    android:textSize="16dp"
                    android:text="Union Station, St. Louis, MO"
                    android:textAppearance="@style/BoldText"/>
                <TextView
                    android:id="@+id/compDateLabel"
                    android:layout_below="@id/compLocLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="26dp"
                    android:textSize="16dp"
                    android:text="April 26-30, 2016"
                    android:textAppearance="@style/BoldText"/>

                <TableLayout
                    android:id="@+id/matchTableMain"
                    xmlns:android="http://schemas.android.com/apk/res/android"
                    android:layout_below="@id/compDateLabel"
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="20dp">


                </TableLayout>

            </RelativeLayout>

        </ScrollView>

    </RelativeLayout>

    <ListView android:id="@+id/navList"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:background="#f5f5f5"/>

</android.support.v4.widget.DrawerLayout>